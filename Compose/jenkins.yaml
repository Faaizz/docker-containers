version: "3.8"

services:
    jenkins:
        image: jenkins/jenkins:lts-jdk11
        volumes:
            - ./jenkins:/var/jenkins_home
        ports:
            - 8080:8080
            - 50000:50000
# username: root
# password: password
